export const getFormatDate = (str) => {
  let dt = new Date(str);

  return `${(dt.getDate()+1).toString().padStart(2, '0')}/${
    dt.getMonth().toString().padStart(2, '0')}/${
    dt.getFullYear().toString().padStart(4, '0')}`;
};

export const getFormatTime = (str) => {
  let dt = new Date(str);

  return `${dt.getHours().toString().padStart(2, '0')}:${
    dt.getMinutes().toString().padStart(2, '0')}:${
    dt.getSeconds().toString().padStart(2, '0')}`;
}

export const theaders = ['№', 'Theme', 'Date', 'From', 'To', 'Time', 'Result'];

export const getDifference = (a, b) => {
  const result = ((Math.abs(new Date(a) - new Date(b))) / 60000).toFixed(2);
  if (result > 60000) {
    return 'Test faild';
  }

  return result;
};

export const getProgessBarValue = (data) => {
  // 10 count of questions in test
  if (data > 0) return `${(data * 100 / 10)}%`;
  if (!data) return '0%';
};